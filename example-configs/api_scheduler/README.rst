=============
API Scheduler
=============

Basic idea of this scheduler is that every new service just appear empty and
unconfigured in the schedule. Then you can use JSON API or GUI buttons to put
services to servers.
